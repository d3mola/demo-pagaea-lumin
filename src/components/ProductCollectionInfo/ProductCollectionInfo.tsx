import { Box } from '@chakra-ui/react';
import React, { ReactElement } from 'react';

export function ProductCollectionInfo(): ReactElement {
    return <Box>Collection Title and Filter Here</Box>;
}
